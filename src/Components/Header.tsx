import {Component, ReactNode } from 'react';

export default class Header extends Component{
    render():ReactNode{
        return(
            <div>
                <h1>header</h1>
            </div>
        )
    }
}
